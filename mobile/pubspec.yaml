name: m_stoic

description: M-STOIC cross-platform trading companion for iOS and Android.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/m-stoic-logo.png
